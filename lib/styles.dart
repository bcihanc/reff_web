import 'package:flutter/cupertino.dart';

const xsmallPadding = EdgeInsets.all(4.0);
const smallPadding = EdgeInsets.all(8.0);
const mediumPadding = EdgeInsets.all(16.0);
const largePadding = EdgeInsets.all(32.0);
